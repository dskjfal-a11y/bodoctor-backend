# 실행 순서 (Windows PowerShell 기준)

## 0. 준비물 확인
- Node.js 설치 여부 확인. 안 깔려있으면 바탕화면의 `node-v24.16.0-x64.msi` 더블클릭 → 설치
- 인터넷 연결

---

## 1. 백엔드 실행

PowerShell 한 줄로:
```powershell
cd "$env:USERPROFILE\OneDrive\Desktop\보험AI백엔드"
Get-NetTCPConnection -LocalPort 8787 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }; Start-Sleep -Seconds 2
node 03_backend_server.js
```

성공 출력:
```
🚀 보험 분석 백엔드 실행: http://localhost:8787
   Gemini: ✓, Pinecone: ✓, CODEF: ✓
```

**이 PowerShell 창은 닫지 마세요.**

---

## 2. (한 번만) Pinecone 인덱스 생성 + 약관 적재

**새 PowerShell 창**을 열고:

```powershell
cd "$env:USERPROFILE\OneDrive\Desktop\보험AI백엔드"

# 2-1. Pinecone 인덱스 생성 (이미 만들었다면 스킵)
node 02b_embed_existing_texts.js --create-index
```

만약 새로 만들었으면, [Pinecone 콘솔](https://app.pinecone.io)에서 인덱스 `test`의 **Host URL**을 복사해서 `.env`의 `PINECONE_HOST=` 값에 붙여넣고, **백엔드 PowerShell 창은 Ctrl+C 후 다시 실행** 해주세요.

```powershell
# 2-2. 약관 10개 적재 (한 번만)
node 02b_embed_existing_texts.js --run

# 또는 기존 데이터 비우고 다시 적재
node 02b_embed_existing_texts.js --run --reset
```

성공 출력 예시:
```
📁 C:\Users\김수찬\OneDrive\Desktop\보험약관
📄 10개 텍스트 파일 발견:
   - 01_samsungfire.txt
   - 02_hyundai.txt
   ...
▶ 01_samsungfire.txt
   보험사: 삼성화재 / 상품: 무배당 삼성화재 다이렉트 실손의료비보험(2605.1) / 세대: 5세대
   5청크 임베딩 중...
   ✓ 5건 upsert 완료
...
✅ 전체 완료: 10개 파일 → 50청크 적재
```

---

## 3. 브라우저로 페이지 열기

```
file:///C:/Users/김수찬/OneDrive/Desktop/insurance-ai-gemini.html
```

상단 우측 배지 → **"백엔드 연결됨"** (초록색) 확인.

---

## 4. 사용 흐름

페이지 상단 **"내 보험 분석받기 →"** 클릭하면 모달에 3개 선택지가 보입니다:

### 🟢 직접 입력 (기본 선택, 가장 빠름)
1. "보험 추가" 버튼으로 카드 1~여러 개 만들기
2. 각 카드에 보험사·유형·상품명·월 보험료·가입년도 입력
3. (선택) 추가 질문 입력
4. **[AI 분석 시작]** → 약관 검색 → AI 분석 결과 + 근거 약관 표시

→ **인증 없이도 동작.** Pinecone 적재 후엔 답변에 근거 약관까지 인용됩니다.

### 🟡 내보험다보여 (credit4u, 자동 조회)
1. 기존 회원이면 → ID/비밀번호 입력 → 조회
2. 신규면 → 본인정보·새 ID/비번/이메일 입력 → SMS 인증 → 자동 조회

### 🔴 간편인증 (마이데이터 자격 필요, 데모비전 등급 ❌)
- 코드에프에서 손보/생보협회 API 추가 활성화 필요
- 현재 데모비전 등급에선 동작 안 함

---

## 5. ⚠️ 작업 끝나면 폐기

- Gemini, Pinecone, 코드에프 키 모두 폐기/재발급
- credit4u에 가입했다면 [credit4u.or.kr](http://credit4u.or.kr/)에서 탈퇴 (또는 그대로 두셔도 됨)
- 채팅에 노출된 모든 비밀번호도 변경

---

## 문제 해결

### `Cannot find module '@google/genai'` 또는 `@pinecone-database/pinecone`
```powershell
npm install
```

### `pdf-parse` 빌드 오류 (Windows)
02b 스크립트는 pdf-parse를 안 쓰지만 백엔드 의존성에 포함됨. 우회:
```powershell
npm install --ignore-scripts
```

### Pinecone `Index host not found` 또는 차원 mismatch
- 인덱스 생성 후 콘솔에서 Host URL 복사해 `.env`의 `PINECONE_HOST=`에 정확히 넣었는지 확인
- 인덱스 차원이 `EMBED_DIM`(기본 3072)과 일치하는지 확인. 다르면 `--reset` 후 인덱스 삭제·재생성 필요

### `EADDRINUSE :::8787`
```powershell
Get-NetTCPConnection -LocalPort 8787 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### 분석 결과에 "Pinecone에 적재된 약관이 없어..." 경고가 보임
2단계(약관 적재)를 실행 안 한 상태. 위 2단계 수행하면 사라집니다.

### 코드에프 `CF-00003` 또는 `CF-09002`
데모비전 등급의 API 활성화 한계. 직접 입력 모드(🟢)로 우회.
